---
phase: 01-dependency-setup-worker-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/types/docx-preview.d.ts
  - src/services/infrastructure/DiagnosticService.ts
  - src/services/infrastructure/__tests__/diagnostic.spec.ts
autonomous: true
requirements:
  - Foundational setup

must_haves:
  truths:
    - "TypeScript compilation finishes without missing type errors for docx-preview"
    - "Diagnostic service correctly performs worker status checks for PDF.js and Tesseract.js"
    - "Vitest unit tests run and pass successfully"
  artifacts:
    - path: "src/types/docx-preview.d.ts"
      provides: "docx-preview custom type definitions"
      contains: "declare module 'docx-preview'"
    - path: "src/services/infrastructure/DiagnosticService.ts"
      provides: "Workers check methods"
      contains: "class DiagnosticService"
    - path: "src/services/infrastructure/__tests__/diagnostic.spec.ts"
      provides: "Unit tests suite for DiagnosticService"
      contains: "describe('DiagnosticService'"
  key_links:
    - from: "src/services/infrastructure/__tests__/diagnostic.spec.ts"
      to: "src/services/infrastructure/DiagnosticService.ts"
      via: "Module import and invocation"
---

<objective>
Tạo file kiểu dữ liệu custom cho docx-preview. Triển khai DiagnosticService nhằm thực hiện kiểm định hạ tầng (PDF.js CDN worker và Tesseract.js worker). Viết unit test tự động bằng Vitest để chứng minh các Service khởi động worker thành công.

Purpose: Đảm bảo hạ tầng Worker client-side hoạt động ổn định và sẵn sàng cho các phase xử lý tài liệu tiếp theo.
Output: Khai báo types, cài đặt DiagnosticService và bộ unit test kiểm thử.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dependency-setup-worker-infrastructure/01-CONTEXT.md
@.planning/phases/01-dependency-setup-worker-infrastructure/01-RESEARCH.md
@.planning/phases/01-dependency-setup-worker-infrastructure/01-PATTERNS.md
@.planning/phases/01-dependency-setup-worker-infrastructure/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add custom declaration for docx-preview</name>
  <files>src/types/docx-preview.d.ts</files>
  <action>
    Tạo file `src/types/docx-preview.d.ts` để khai báo kiểu TypeScript cho module `docx-preview`.
    Khai báo export hàm `renderAsync(document: any, bodyContainer: HTMLElement, styleContainer?: HTMLElement, options?: any): Promise<any>`.
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit</automated>
  </verify>
  <done>Khai báo type tồn tại và trình biên dịch TS không còn báo lỗi khi import 'docx-preview'.</done>
</task>

<task type="auto">
  <name>Task 2: Implement DiagnosticService and Vitest tests</name>
  <files>src/services/infrastructure/DiagnosticService.ts, src/services/infrastructure/__tests__/diagnostic.spec.ts</files>
  <action>
    Viết class `DiagnosticService` trong `src/services/infrastructure/DiagnosticService.ts`. Class cung cấp hai phương thức static:
    1. `checkPdfWorker()`: cấu hình PDF.js workerSrc trỏ về URL CDN Cloudflare `https://cdnjs.cloudflare.com/ajax/libs/pdf.js/5.7.284/pdf.worker.min.mjs` và nạp thử tài liệu giả lập.
    2. `checkTesseractWorker()`: import dynamic `tesseract.js`, chạy thử khởi tạo worker với ngôn ngữ `eng` và đóng worker lại để kiểm chứng trạng thái chạy WASM.
    Viết file test `src/services/infrastructure/__tests__/diagnostic.spec.ts` sử dụng `vitest` (`describe`, `it`, `expect`, `beforeAll`...) để gọi kiểm tra hai chức năng chẩn đoán này.
  </action>
  <verify>
    <automated>npx vitest run src/services/infrastructure/__tests__/diagnostic.spec.ts</automated>
  </verify>
  <done>DiagnosticService và unit test được triển khai thành công, kết quả kiểm thử báo PASS hoàn toàn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser Runtime -> External CDN | Tải các mã nguồn worker của PDF.js và file ngôn ngữ của Tesseract.js. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Spoofing | CDN Worker scripts | mitigate | Sử dụng giao thức bảo mật HTTPS và chỉ rõ số phiên bản cụ thể (5.7.284) để tránh việc lấy các phiên bản worker chưa xác thực. |
</threat_model>

<verification>
Chạy bộ test Vitest chẩn đoán thành công.
</verification>

<success_criteria>
Các test case chạy qua, kiểu dữ liệu khớp, build app Vue sạch sẽ.
</success_criteria>

<output>
After completion, create `.planning/phases/01-dependency-setup-worker-infrastructure/01-02-SUMMARY.md`
</output>
